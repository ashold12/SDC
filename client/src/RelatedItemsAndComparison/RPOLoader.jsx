import React from 'react';

function RPOLoader() {
  const style = { fontSize: '16px' };

  return (
    <div className="spinner" style={style}>
      <div className="head">
    </div>
    </div>
  );
}

export default RPOLoader;
